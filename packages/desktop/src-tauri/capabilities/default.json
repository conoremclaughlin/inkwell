{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the bundled fallback page in the main window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
