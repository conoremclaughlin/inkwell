/**
 * Type stub for Claude Code's render-node-to-output.ts
 *
 * The full file depends on React/Yoga layout nodes we don't use.
 * Only the ScrollHint type is needed by frame.ts.
 */

export type ScrollHint = { top: number; bottom: number; delta: number };
