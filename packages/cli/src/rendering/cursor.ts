/**
 * Cursor type — extracted from Claude Code's rendering engine.
 *
 * In the fork this type lives elsewhere (not a standalone file).
 * Created as a stub so frame.ts's `import type { Cursor }` resolves.
 */

export type Cursor = {
  x: number;
  y: number;
  visible: boolean;
};
